import React, { Fragment } from 'react';

const About = () => {
	return (
		<Fragment>
			<h1>About this app</h1>
			<p>this app uses github api to find github users</p>
			<p>all github users can be querried using the search box!</p>
		</Fragment>
	);
};
export default About;
